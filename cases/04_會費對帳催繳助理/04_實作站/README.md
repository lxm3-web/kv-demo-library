# 04 實作站 — Google Sheet 選單（自動化串接版）

`Code.gs`：對帳 → 帳齡分級 → 催繳優先清單 → 重置。改寫自 Case 08（怡臻）應收帳款 GAS。
安裝：open@ 建一份 Sheet「會費對帳催繳（demo）」→ 匯入 `01_輸入假資料/會員名冊_2026.csv` 為分頁「會員名冊」、`銀行入帳明細_2026.csv` 為分頁「銀行入帳」→ 擴充功能 → Apps Script → 貼 Code.gs → 儲存 → 重整。
Sheet 網址：待建後填入設定卡。
